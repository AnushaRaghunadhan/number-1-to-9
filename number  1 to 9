#include<stdio.h>

int main() 
{
   int num;
   scanf("%d",&num);
   if(num>=9||num<=1)
   {
       printf("enter the number between 1 to 9 ");
   }
   else
   {
       printf("the num is %d",num);
   }
   

   return (0);
}
